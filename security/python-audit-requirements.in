# External dependencies declared by first-party Python packages and integrations.
# Build and development dependencies are included because they execute in CI
# and release environments.
hatchling
httpx>=0.27.0
langchain>=0.2
langchain-core>=0.2
pydantic>=2.0
pytest>=8.0
pytest-asyncio>=0.23
setuptools>=68.0
websockets>=12.0
wheel
